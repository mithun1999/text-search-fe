import { Button } from "@mui/material";
import { useGetAllCategoryQuery } from "./query/category.query";
import { tw } from "twind";

function Post() {

  return (
    <div>
      
    </div>
  );
}

export default Post;
